# VSEC Redis 安全配置
bind 0.0.0.0
port 6379
# 设置密码（修改为你的强密码；Docker 部署由 docker-compose 环境变量覆盖）
requirepass changeme

# 安全：关闭持久化（防止密钥落盘）
save ""
appendonly no

# 内存限制
maxmemory 256mb
maxmemory-policy allkeys-lru

loglevel notice
